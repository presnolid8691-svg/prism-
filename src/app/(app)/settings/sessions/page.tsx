import { SessionManager } from '@/components/settings/SessionManager'

export const metadata = { title: 'Active Sessions — Prism' }

export default function SessionsPage() {
  return <SessionManager />
}